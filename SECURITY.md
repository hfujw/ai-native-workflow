# 安全说明

> 本地开源桌面工具——请把 API Key 当密码保管。

## 本项目已做的防护（本地场景足够）

- **生成的页面隔离**：产物 HTML 在前端用 `sandbox="allow-scripts"` iframe 预览，不接触宿主 DOM
- **提示注入防御**：搜索/外部素材里的注入特征（"ignore previous instructions" 等）会被检测并降级
- **输入限长**：主题与迭代指令都限制长度，防误粘贴超长文本烧 API 预算
- **单次预算上限**：每次生成默认 ≤ ¥1（真实 token 按模型费率表记账，可配置），迭代计入同一预算
- **断路器**：LLM API 连续失败自动熔断 30 秒，防级联
- **日志轮转**：本地日志保留 30 天自动清理；`LOG_PROMPTS=1` 才会记录完整 prompt

## 不承诺的事（欢迎 PR）

本项目是纯开源个人项目，**不是面向公网的服务**：没有用户认证、没有传输加密（本地回环）、没有多租户隔离。请勿把它部署为公开服务。
